export const CONFIG = {
  /**
   * In a real-world application, these values would be stored in a secure location.
   * like environment variables, secure vaults, or other secure storage mechanisms.
   *
   * However, for the sake of simplicity, we are storing them directly in the configuration file.
   * This is not recommended for production applications. This is only for demonstration purposes.
   */
  API_URL: 'https://jsonplaceholder.typicode.com',
  ITEMS_PER_PAGE: 3,
};